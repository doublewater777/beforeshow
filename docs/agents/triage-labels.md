# Triage Labels

Use these exact label strings when triaging or creating issues:

- `needs-triage` — issue needs maintainer evaluation
- `needs-info` — waiting on reporter for more information
- `ready-for-agent` — fully specified, an AFK agent can pick it up
- `ready-for-human` — needs human implementation or decision
- `wontfix` — will not be actioned
