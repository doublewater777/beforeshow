# Screenshots

Only **curated final evidence** for product/UI decisions.

Do **not** commit:

- intermediate exploration dumps (`route-v3/`, numbered verification runs, failed open attempts)
- simulator debug frames, terminal OCR captures
- near-duplicate before/after drafts (keep one final)

Agent verification runs may write under a local path that is gitignored; promote only the final frame(s) here when needed.
