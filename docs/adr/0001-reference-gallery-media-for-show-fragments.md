# Reference gallery media for show fragments

BeforeShow will keep show fragment media lightweight by referencing photos and videos from the user's system gallery instead of copying original files into the app. Recordings created inside the app are stored by the app, attached to the relevant show fragment, and deleted when the user deletes that content; this keeps storage use predictable while accepting that gallery references can disappear if the user removes the original media.
