# Use on-device OCR for show screenshots

BeforeShow will treat ticketing screenshots as sensitive user material and use on-device OCR first when assisting users with adding a show. Screenshots are not uploaded for recognition in V2.1; OCR results are only draft field values that the user must confirm or edit, and recognition failure falls back to manual entry.
