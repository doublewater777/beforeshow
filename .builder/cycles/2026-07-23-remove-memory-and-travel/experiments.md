# Experiments

This is a scope decision before launch, so verification is implementation-focused:

- Search the active repository for feature identifiers and user-facing terms.
- Run the remaining iOS and CloudBase test suites.
- Launch the app on iPhone 17 and inspect the current-show home.

Pass threshold:

- Zero active source references to fragment or travel domain types.
- Zero generation backend or deployment configuration for retired AI features.
- All remaining tests pass.
- Home contains no retired entry or copy.

Loop back if deleting these features breaks show creation, selection, lifecycle notifications, or the countdown.
