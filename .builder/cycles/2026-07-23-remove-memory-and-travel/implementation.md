# Implementation

Remove:

- Memory-fragment models, media/audio services, UI, permissions, tests, plans, and copy.
- Departure/round-trip models, MapKit planning UI, generation contracts, tests, plans, and copy.
- The now-unused generic AI generation CloudBase function, provider clients, deployment scripts, and model configuration.
- Fake-door and App Store material that promises either feature.
- Feature-specific design tokens and notification language.

Keep:

- Show creation, editing, current-show selection, lifecycle state, countdown, notifications, saved-show limit, and link parsing.
- Historical evidence only where explicitly marked superseded by the retirement ADR.
