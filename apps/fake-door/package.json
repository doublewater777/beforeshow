{
  "name": "@beforeshow/fake-door",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "deploy:pages": "npm run build && npx wrangler pages deploy dist --branch main",
    "export:leads": "node scripts/export-leads.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "proxy:feishu": "node scripts/feishu-lead-proxy.mjs",
    "setup:feishu": "bash scripts/setup-feishu-bitable.sh",
    "start": "node scripts/production-server.mjs",
    "start:prod": "npm run build && node scripts/production-server.mjs"
  },
  "dependencies": {
    "@beforeshow/core": "0.1.0",
    "@beforeshow/ui": "0.1.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vite": "^5.4.11"
  }
}
